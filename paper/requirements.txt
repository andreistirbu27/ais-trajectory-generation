# Paper-track Python dependencies.
#
# Install into a separate venv (e.g. `paper/.venv/`) to keep school-report
# environment (`requirements.txt` at repo root) untouched.
#
# Setup:
#   python3 -m venv paper/.venv
#   source paper/.venv/bin/activate
#   pip install --upgrade pip
#   pip install -r paper/requirements.txt
#
# Note on TrAISformer: their published `paper/external/TrAISformer/requirements.yml`
# is a Python 3.7 / CUDA 9.2 conda dump and is not usable on modern GPUs.
# Reading their code, the actual runtime deps are minimal: torch, numpy,
# matplotlib, tqdm, (optional) tensorboard. All covered below.
#
# v13 (diffusion) adds nothing extra; reuses the same torch / numpy install.

# --- Core ML (shared with v10) ---
torch>=2.1
numpy>=1.24,<2.0          # geopandas wheels pin <2.0; keep until 2026 cycle
pandas>=2.0
scipy>=1.10

# --- Plotting + reports ---
matplotlib>=3.7
seaborn>=0.13             # used in some paper figures
tqdm>=4.65

# --- Geo + maps ---
geopandas>=0.13           # v10 land-mask SDF construction
rasterio<1.5              # locked by v10 numpy<2 constraint
shapely>=2.0
contextily>=1.4           # map tiles in viz

# --- ML utilities ---
scikit-learn>=1.3
pyyaml>=6.0
einops>=0.7               # cleaner shape manipulations in v13 backbone

# --- Diffusion specifics (for v13) ---
# No extra packages needed; we implement the sampler ourselves to avoid
# pulling in `diffusers` (which would constrain torch versions and add 100+ MB
# of optional deps we won't use).

# --- TrAISformer baseline ---
tensorboard>=2.13         # optional; gated by their `tb_log` flag

# --- Dev / experiment tracking ---
# (none yet; add jupyter/wandb here if needed)

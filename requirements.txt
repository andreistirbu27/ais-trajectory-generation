# Base (school-report + v1–v12 + production v10) dependencies.
# Pinned to the verified-working versions (captured 2026-06-28, Python 3.9).
# numpy is hard-pinned <2.0: the geo stack (geopandas/rasterio) wheels require
# it and the saved scalers assume it. rasterio stays <1.5 for the same reason.
# The paper track (Python 3.9 + torch 2.8 + diffusion) uses paper/requirements.txt.
torch==2.8.0
pandas==2.3.3
numpy==1.26.4
scipy==1.13.1
matplotlib==3.9.4
tqdm==4.67.3
PyYAML==6.0.3
contextily==1.6.2
rasterio==1.4.3        # <1.5, locked by numpy<2 constraint
scikit-learn==1.6.1
geopandas==1.0.1
